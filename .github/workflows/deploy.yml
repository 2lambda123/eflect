name: deploy

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - README.md

permissions:
  contents: read

jobs:
   use-build:
    uses: UM-LPM/eflect/.github/workflows/build.yml@main

  deploy:
    needs: use-build
    
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: recursive
